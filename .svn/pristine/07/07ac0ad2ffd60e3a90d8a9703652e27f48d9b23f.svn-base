#include "rect.h"

float Rect::getCenterX() const {
	return x + w / 2.0f;
}

float Rect::getCenterY() const {
	return y + h / 2.0f;
}

void Rect::setCenter(float cx, float cy) {
	x = cx - w / 2.0f;
	y = cy - h / 2.0f;
}